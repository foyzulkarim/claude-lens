/**
 * Temporary placeholder — gives `tsc --noEmit` a real input in this root.
 * Exercises `@types/node` via `types: ["node"]`; removed by #P1-2 (cli/app).
 */
export const NODE_VERSION: string = process.version;
