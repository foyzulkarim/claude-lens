/**
 * Temporary placeholder — gives `tsc --noEmit` a real input in this root.
 * Removed by #P2-1 when the shared contracts (types / ws-protocol) land.
 */
export const SHARED_ROOT: string = "shared";
